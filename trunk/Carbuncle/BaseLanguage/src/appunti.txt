Non riconosce lo 0 come cifra quindi se lo si mette come numero di battuta non viene visualizzato alcun
errore ma non viene portato a termine il parsing; è inutile scrivere if(num==0)...

lo 0 non mi serve mai, l'ho messo solo per vedere se c'è un errore di inserimento (errore sintattico)
Modifiche da fare alla grammatica:
1) togliere strikeout::= <K> <- lo lascio perchè i token hanno nomi facilmente comprensibili, a differenza del simbolo usato per rappresentarli,
	ad esempio OUT_VOLO è mappato 1 a 1 con F ma non per questo lo tolgo
2) la media battuta è sempre a 0, forse devo aggiornare il giocatore nel vettore e non solo il battitore(Casa/Ospite),
	ma dove?

